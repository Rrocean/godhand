# GodHand Pro v3.0 🖐️

**统一智能命令与GUI自动化系统**

---

## ✨ 核心改进

### v3.0 新特性

| 特性 | 描述 |
|------|------|
| 🎯 **意图识别** | 智能分类用户意图，自动选择最佳执行策略 |
| 🧠 **上下文记忆** | 记住用户习惯和常用命令，提升响应准确度 |
| ⚡ **统一执行引擎** | 融合 Claw (后台命令) 和 Ghost (GUI自动化) 模式 |
| 📊 **性能监控** | 完整的执行统计、错误分析和性能报告 |
| 🗄️ **元素库** | 缓存常用UI元素，加速视觉识别 |
| 🎨 **全新UI** | 现代化的深色主题界面，更好的交互体验 |

---

## 🚀 快速开始

### 1. 安装依赖

```bash
cd godhand
pip install -r requirements.txt
```

### 2. 配置API密钥

编辑 `config.json`：

```json
{
  "provider": "google",
  "google": {
    "api_key": "your-google-api-key",
    "model": "gemini-2.0-flash"
  },
  "openai": {
    "api_key": "your-openai-api-key",
    "model": "gpt-4o"
  }
}
```

或设置环境变量：
```bash
set GOOGLE_API_KEY=your-api-key
set OPENAI_API_KEY=your-api-key
```

### 3. 启动服务

**方式1: 使用启动脚本**
```bash
start_pro.bat
```

**方式2: 直接启动**
```bash
python main_v2.py
```

访问 http://127.0.0.1:8000

---

## 📖 使用指南

### 复合指令示例

```
打开记事本 输入Hello World 然后保存
打开计算器 计算1+1 截图结果
在浏览器搜索Python教程 打开第一个结果
点击开始菜单 搜索设置 打开系统设置
```

### 执行模式

| 模式 | 说明 | 适用场景 |
|------|------|----------|
| `auto` | 自动选择最佳方式 | 大多数情况 |
| `command` | 后台命令执行 | 打开应用、文件操作 |
| `gui` | GUI自动化 | 点击、输入、视觉操作 |

### WebSocket 实时通信

```javascript
const ws = new WebSocket('ws://localhost:8000/ws/session_123');
ws.send(JSON.stringify({
    message: "打开记事本 输入Hello",
    mode: "auto"
}));
```

---

## 🏗️ 项目结构

```
godhand/
├── core/                       # 核心引擎
│   ├── ghost_v3.py            # GUI自动化核心 (v3增强版)
│   ├── smart_parser_v2.py     # 智能解析器 (v2增强版)
│   ├── claw_runner.py         # 后台命令执行器
│   ├── monitor.py             # 性能监控
│   └── elements/              # 元素库
│
├── web/                        # Web界面
│   ├── templates/
│   │   └── index.html         # 主页面
│   └── static/
│       ├── css/
│       │   └── style.css      # 样式
│       └── js/
│           └── app.js         # 前端逻辑
│
├── data/                       # 数据存储
│   ├── cache/                 # 缓存数据
│   ├── logs/                  # 日志文件
│   ├── screenshots/           # 截图保存
│   ├── element_library.json   # 元素库
│   ├── metrics.db             # 性能指标数据库
│   └── parser_learning.json   # 解析器学习数据
│
├── main_v2.py                 # 主应用 (FastAPI)
├── config.json                # 配置文件
├── requirements.txt           # 依赖
└── start_pro.bat             # Windows启动脚本
```

---

## 📡 API 参考

### 执行指令
```bash
POST /api/execute
Content-Type: application/json

{
    "command": "打开计算器",
    "mode": "auto",
    "session_id": "optional-session-id"
}
```

### 仅解析
```bash
POST /api/parse
Content-Type: application/json

{
    "command": "打开记事本 输入Hello"
}
```

### 获取统计
```bash
GET /api/stats
```

### 健康检查
```bash
GET /api/health
```

---

## 📊 性能监控

监控数据存储在 SQLite 数据库中，可以通过以下方式查看：

```python
from core.monitor import get_monitor

monitor = get_monitor()
monitor.print_summary()  # 打印24小时摘要

# 生成完整报告
report = monitor.generate_report(days=7)
```

---

## 🔧 高级配置

### 安全配置
```json
{
  "safety": {
    "enabled": true,
    "max_steps": 30,
    "step_delay": 1.0,
    "click_delay": 0.3
  }
}
```

### 截图配置
```json
{
  "screenshot": {
    "save_dir": "./data/screenshots",
    "show_grid": false,
    "grid_size": 100
  }
}
```

---

## 📝 更新日志

### v3.0 (2026-02-06)
- ✨ 全新统一执行引擎
- 🧠 智能意图识别与分类
- 💾 元素库缓存系统
- 📊 完整性能监控体系
- 🎨 现代化Web界面
- 🔄 上下文记忆功能

### v2.0
- 🤖 GhostHand Pro GUI自动化
- 🌐 WebSocket实时通信
- 🎯 复合指令解析

---

## ⚠️ 安全提示

- GUI自动化会实际控制鼠标和键盘
- 设置了安全保护：快速将鼠标移到屏幕角落可中断执行
- 建议在测试环境先验证指令

---

## 📄 License

MIT License
