# GodHand 🌌

**宇宙第一的智能GUI自动化系统**

[![Version](https://img.shields.io/badge/version-v3.0.0--universe-blue.svg)](./)
[![Status](https://img.shields.io/badge/status-宇宙级标准达成-brightgreen.svg)](./docs/UNIVERSE_FIRST_ACHIEVED.md)
[![Python](https://img.shields.io/badge/python-3.8+-blue.svg)](https://python.org)

把自然语言指令拆成可执行步骤，支持命令模式、视觉GUI自动化、AI自主代理、语音控制、云端协作。

> 🏆 **宇宙级标准达成** - 查看 [成就宣告](./docs/UNIVERSE_FIRST_ACHIEVED.md)

## ✨ 宇宙级核心特性

- 🧠 **AI自主代理**：具备长期记忆和反思能力的智能代理
- 🎤 **语音控制**：完全免手的语音交互体验
- ☁️ **云端同步**：多设备同步，团队协作
- 🧠 **智能解析**：支持复合指令、多步拆解
- 🤖 **双引擎执行**：后台命令 + 视觉GUI自动化
- 👁️ **视觉理解**：基于CV的UI元素智能检测与定位
- 📚 **自主学习**：从用户演示中自动学习工作流
- 🌐 **Web 控制台**：实时 WebSocket 通信与执行状态
- 🔌 **插件系统**：可扩展的插件架构
- 🛡️ **错误恢复**：智能错误分类与自动恢复
- 📊 **性能监控**：实时性能监控与报告
- 🖥️ **跨平台**：Windows/Mac/Linux 统一支持
- 🛠️ **IDE集成**：VSCode 原生插件

## 🚀 快速开始

### 1. 安装依赖

```bash
cd godhand
pip install -r requirements.txt
```

### 2. 配置 API 密钥

编辑 `config.json`：

```json
{
  "provider": "google",
  "google": {
    "api_key": "your-google-api-key",
    "model": "gemini-2.5-pro"
  },
  "openai": {
    "api_key": "your-openai-api-key",
    "model": "gpt-4o",
    "base_url": "https://api.openai.com/v1"
  },
  "safety": {
    "enabled": true,
    "max_steps": 20,
    "step_delay": 1.0,
    "click_delay": 0.5
  },
  "screenshot": {
    "save_dir": "./screenshots",
    "show_grid": false,
    "grid_size": 100
  }
}
```

或通过环境变量：
```bash
set GOOGLE_API_KEY=your-api-key
set OPENAI_API_KEY=your-api-key
```

### 3. 启动服务

**宇宙版（v3.0 推荐）**：
```bash
python main_v3.py
```

访问: http://127.0.0.1:8000

## 🌌 宇宙级功能架构

```
┌─────────────────────────────────────────────────────────────┐
│                    GodHand v3.0 宇宙版                       │
│                  The Universe's #1 GUI Automation           │
└─────────────────────────────────────────────────────────────┘
                            │
        ┌───────────────────┼───────────────────┐
        ▼                   ▼                   ▼
   ┌─────────┐        ┌─────────┐        ┌─────────┐
   │  AI代理  │        │ 语音控制 │        │ 云端同步 │
   │ AIAgent │        │ Voice   │        │ Cloud   │
   └─────────┘        └─────────┘        └─────────┘
        │                   │                   │
        └───────────────────┼───────────────────┘
                            ▼
              ┌─────────────────────────┐
              │    VisualEngine         │
              │    TaskPlanner          │
              │    LearningSystem       │
              │    SmartParser          │
              └─────────────────────────┘
                            │
        ┌───────────────────┼───────────────────┐
        ▼                   ▼                   ▼
   ┌─────────┐        ┌─────────┐        ┌─────────┐
   │ 错误恢复 │        │ 性能监控 │        │ 插件系统 │
   └─────────┘        └─────────┘        └─────────┘
```

## 🚀 宇宙级功能演示

### AI 自主代理
```python
from core import AIAgent

agent = AIAgent(name="My Agent")
agent.register_skill("open_app", lambda target: {"output": f"打开 {target}"})

# AI 自动规划并执行
result = agent.run("打开浏览器，搜索 Python 教程")
```

### 语音控制
```python
from core import VoiceController

vc = VoiceController()
vc.interactive_mode()
# 说出命令，系统自动识别并执行
```

### 云端同步
```python
from core import CloudSync, CollaborationRole

sync = CloudSync()
sync.register_device({"name": "User", "email": "user@example.com"})
sync.share_workflow("wf_001", "自动备份", steps=[...])
sync.invite_team_member("team@example.com", "Member", CollaborationRole.EDITOR)
```

### 视觉理解
```python
from core import VisualEngine

engine = VisualEngine()
elements = engine.detect_elements(screenshot)
target = engine.locate_element("保存按钮", screenshot)
```

## 📖 使用指南

## 📖 使用指南

### 复合指令示例

- 打开记事本 输入 Hello World 保存到桌面
- 打开计算器 计算 1+1 并截图
- 在浏览器搜索 Python 教程 打开第一个结果

### 执行模式

| 模式 | 说明 | 适用场景 |
|------|------|----------|
| `auto` | 自动选择执行方式 | 大多数场景 |
| `command` | 后台命令执行 | 打开应用、文件操作 |
| `gui` | 视觉GUI自动化 | 点击、输入、视觉操作 |

## 📡 API 参考

### 执行指令
```bash
POST /api/execute
Content-Type: application/json

{
  "command": "打开计算器",
  "mode": "auto",
  "session_id": "optional-session-id"
}
```

### 仅解析（不执行）
```bash
POST /api/parse
Content-Type: application/json

{
  "command": "打开记事本 输入Hello"
}
```

### 对话模式
```bash
POST /api/chat
Content-Type: application/json

{
  "command": "打开计算器",
  "session_id": "optional-session-id"
}
```

### WebSocket
```javascript
const ws = new WebSocket('ws://localhost:8000/ws/session_123');
ws.send(JSON.stringify({
  message: "打开计算器",
  mode: "auto"
}));
```

### 健康检查
```bash
GET /api/health
```

## 🏗️ 项目结构

```
godhand/
├── core/               # 核心引擎
├── web/                # Web界面
│   ├── templates/      # 页面模板
│   └── static/         # 静态资源
├── main.py             # 标准版 FastAPI
├── main_v2.py          # Pro 版 FastAPI
├── config.json         # 配置文件
└── requirements.txt    # 依赖
```

## ⚠️ 安全提示

- GUI 自动化会实际控制鼠标和键盘
- 支持紧急中断：将鼠标快速移到屏幕角落
- 建议先在测试环境验证指令

## 📄 License

MIT License
