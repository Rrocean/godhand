# GodHand 🖐️

**智能命令执行与GUI自动化系统**

整合版：系统命令执行 + 视觉GUI自动化 + Web控制界面

## ✨ 功能特性

- 🖥️ **系统命令模式**: 自然语言转系统命令（打开应用、创建文件等）
- 🤖 **GUI自动化模式**: 基于视觉的GUI操作（点击、输入、截图分析）
- 🌐 **Web界面**: 美观的Web UI，支持WebSocket实时通信
- 🧠 **AI驱动**: 支持 Google Gemini 和 OpenAI GPT-4o 多模态模型
- 📷 **视觉识别**: 屏幕截图分析、坐标定位、元素识别

## 🚀 快速开始

### 1. 安装依赖

```bash
cd godhand
pip install -r requirements.txt
```

### 2. 配置API密钥

编辑 `config.json`：

```json
{
  "provider": "google",
  "google": {
    "api_key": "your-google-api-key",
    "model": "gemini-2.0-flash"
  },
  "openai": {
    "api_key": "your-openai-api-key",
    "model": "gpt-4o",
    "base_url": "https://api.openai.com/v1"
  },
  "safety": {
    "max_steps": 30,
    "step_delay": 0.5
  },
  "screenshot": {
    "save_dir": "./screenshots",
    "show_grid": false
  }
}
```

或通过环境变量设置：
```bash
set GOOGLE_API_KEY=your-api-key
set OPENAI_API_KEY=your-api-key
```

### 3. 启动服务

```bash
python main.py
```

访问: http://127.0.0.1:8000

## 📖 使用指南

### Web 界面

1. 打开浏览器访问 http://127.0.0.1:8000
2. 选择模式：
   - **系统命令**: 执行系统命令（打开应用、搜索等）
   - **GUI自动化**: 基于视觉的自动化操作

### 系统命令示例

| 输入 | 效果 |
|-----|------|
| `打开计算器` | 启动计算器 |
| `打开记事本` | 启动记事本 |
| `创建文件夹 Test` | 创建文件夹 |
| `打开 Chrome` | 启动浏览器 |
| `搜索 Python 教程` | 必应搜索 |

### GUI自动化示例

| 输入 | 效果 |
|-----|------|
| `打开计算器` | 视觉识别并点击打开计算器 |
| `点击开始菜单` | 点击Windows开始按钮 |
| `在记事本中输入Hello` | 打开记事本并输入文字 |
| `截图` | 截取当前屏幕 |

### API 使用

#### 执行系统命令
```bash
curl -X POST http://127.0.0.1:8000/api/execute \
  -H "Content-Type: application/json" \
  -d '{"command": "打开计算器"}'
```

#### 执行GUI自动化
```bash
curl -X POST http://127.0.0.1:8000/api/automation \
  -H "Content-Type: application/json" \
  -d '{"instruction": "打开计算器"}'
```

#### WebSocket 连接
```javascript
const ws = new WebSocket('ws://localhost:8000/ws/session_123');
ws.send(JSON.stringify({
    message: "打开计算器",
    mode: "automation"
}));
```

## 🏗️ 项目结构

```
godhand/
├── core/               # 核心引擎
│   ├── __init__.py
│   ├── ghost_v2.py    # GUI自动化核心 (GhostHand Pro)
│   ├── claw_runner.py # 命令解析执行器
│   ├── ghost_agent.py # Agent实现
│   └── ...
├── web/               # Web界面
│   ├── __init__.py
│   └── static/        # 静态资源
├── examples/          # 示例代码
├── screenshots/       # 截图保存目录
├── main.py           # FastAPI主应用
├── config.json       # 配置文件
└── requirements.txt  # 依赖
```

## ⚠️ 安全提示

- GUI自动化会实际控制鼠标和键盘
- 设置了安全保护：快速将鼠标移到屏幕角落可中断执行
- 首次运行建议在测试环境验证

## 📝 依赖项目

- [GhostHand](ghosthand/) - 原始GUI自动化项目
- [OpenClaw Web](openclaw_web/) - Web界面项目

## 📄 License

MIT License
